# Flask secret key — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# Microsoft 365 / Azure AD (leave blank to disable Microsoft sign-in)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=

# Google Workspace (leave blank to disable Google sign-in)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Comma-separated list of admin email addresses
ADMIN_EMAILS=admin@example.org

# Comma-separated list of allowed email domains (leave blank to allow all)
ALLOWED_DOMAINS=example.org

# Host port (default: 5000)
PORT=5000
